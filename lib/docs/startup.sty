\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{lib/docs/startup}[2019 LMDB doc]

\RequirePackage{etoolbox}

% Old stuff
\comment{
    \newcounter{cnt}

    \newcommand\settext[2]{%
        \csdef{text#1}{#2}}
    \newcommand\addtext[1]{%
        \stepcounter{cnt}%
        \csdef{text\thecnt}{#1}}
    \newcommand\gettext[1]{%
        \csuse{text#1}}

    \newcounter{colnum}
    \newcommand\maketabularrow[1]{%
        \setcounter{colnum}{0}%
        \whileboolexpr
        { test {\ifnumcomp{\value{colnum}}{<}{#1}} }%
        {&\stepcounter{colnum}\thecolnum}
    }
}

\initSet{StartupTitles}
\addVariableToSet{StartupTitles}{Опис ідеї проекту}
\addVariableToSet{StartupTitles}{Технологічний аудит ідеї проекту}
\addVariableToSet{StartupTitles}{Аналіз ринкових можливостей запуску стартап-проекту}
\addVariableToSet{StartupTitles}{Розроблення ринкової стратегії проекту}
\addVariableToSet{StartupTitles}{Розроблення маркетингової програми стартап-проекту}

\newcounter{popupTitleCnt}
\setcounter{popupTitleCnt}{0}

\newcommand{\startStartupSection}{
    \subsection{\getVariableFromSet{StartupTitles}{\thepopupTitleCnt}}
    \stepcounter{popupTitleCnt}
}